version: 2.1

setup: true

orbs:
  path-filtering: circleci/path-filtering@0.0.2

workflows:
  setup-workflow:
    jobs:
      - path-filtering/filter:
          mapping: |
            .circleci/.* circle-ci true
            .src/.* build-client true
          # Optional, defaults to main:
          base-revision: origin/main